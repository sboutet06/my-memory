"""Corrections framework — human-in-the-loop overlay layer.

Three correction layers share this framework:
  - source:     ingestion-layer doubts + overlay (this phase)
  - derivation: graph-layer overrides (future)
  - memory:     workflow-layer rules (future)

See docs/intent.md § "Human-in-the-loop architecture".
"""
