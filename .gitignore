# Personal data — never commit
raw/
raw-*/
# Public-domain / synthetic corpora — committed (no PII)
!raw-synthetic/
!raw-ocr/
!raw-medical/
store/

# Secrets
.env
.env.*
!.env.example

# Python
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Spike outputs (vector DBs, extracted graphs, logs)
spikes/*/output/
spikes/lightrag_out/
lightrag.log

# Extraction store (KG data, auto-generated)
extraction_store/

# Runtime artifacts
extraction_store/
extraction_store.bak/
corrections/source/
corrections/derivation/
facts/store/

# Benchmark run outputs contain LLM answers over the personal corpus —
# real PII (SSN, fiscal IDs, addresses, etc.). Never commit.
benchmarks/runs/
